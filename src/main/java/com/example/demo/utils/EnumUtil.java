package com.example.demo.utils;

public class EnumUtil {
    public enum State {
        RUNNING,
        PAUSED,
        STOP,
    }

    public enum SceneType {
        LEVEL1,
        LEVEL2,
    }
}